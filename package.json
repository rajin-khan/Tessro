{
    "name": "tessro-monorepo",
    "private": true,
    "version": "1.0.0",
    "scripts": {
      "postinstall": "cd client && npm install && npm run build && cd ../server && npm install",
      "start": "cd server && npm run start",
      "dev": "cd server && npm run dev"
    },
    "engines": {
      "node": ">=18"
    }
  }  