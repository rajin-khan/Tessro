# Server Configuration
PORT=3001
CLIENT_URL=http://localhost:5173

# Cloudflare TURN Configuration
# Get these values from: https://dash.cloudflare.com -> Your Account -> Calls -> TURN Keys
# Create a TURN key first, then copy the uid (key ID) and key (API token)
CLOUDFLARE_TURN_KEY_ID=your_turn_key_id_here
CLOUDFLARE_TURN_API_TOKEN=your_turn_api_token_here

# Note: Never commit the actual .env file with real credentials!
